'use strict';

describe('regattaViewer.seriesstanding module', function() {

  beforeEach(module('regattaViewer.seriesstanding'));

  describe('seriesstanding controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('seriesstandingController');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});